---
- hosts: ["all"]
  become: yes
  gather_subset: min
  vars_files:
    - vars/dummy_openvpn_server_secrets.yml
  roles:
    - name: uninstall_openvpn
      tag: uninstall
    - name: install_openvpn
      tag: install
