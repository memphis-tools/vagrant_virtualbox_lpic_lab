---
- hosts: all
  become: yes
  gather_subset: min
  vars_files:
    - vars/dummy_kdc_secrets.yml
    - vars/dummy_users_secrets.yml
  roles:
    - name: uninstall_kerberos_client
      tags: uninstall
    - name: install_kerberos_client
      tags: install
