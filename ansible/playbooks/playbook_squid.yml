---
- hosts: all
  become: yes
  gather_subset: min
  vars_files:
    - vars/dummy_squid_secrets.yml
  roles:
    - name: uninstall_squid
      tags: uninstall
    - name: install_squid
      tags: install
