---
- hosts: all
  become: yes
  gather_subset:
    - '!all'
    - '!min'
    - distribution
    - distribution_version
    - os_family
  vars_files:
    - vars/dummy_ansible_secrets.yml
  roles:
    - name: uninstall_bastion_pubkey_signer
      tags: uninstall
    - name: install_bastion_pubkey_signer
      tags: install
