# Set disable_plaintext_auth to yes, so Dovecot rejects PLAIN or LOGIN authentication unless TLS is active
# Notice not necessary as long as we only allow pop3s and imaps
disable_plaintext_auth = yes
auth_mechanisms = plain login
!include auth-system.conf.ext
