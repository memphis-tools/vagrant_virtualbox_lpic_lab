passdb {
  driver = pam
}
passdb {
  driver = passwd
}
passdb {
  driver = shadow
}
#userdb {
#  driver = passwd
#}

userdb {
  driver = static
  args = uid=vmail gid=vmail home=/var/mail/vhosts/%n
}
