# Syntax for /home/somebody/***
#mail_location = maildir:~/Maildir
namespace inbox {
  inbox = yes
}
mail_privileged_group = mail
protocol !indexer-worker {
}
# Syntax for the no-home needed
mail_location = maildir:/var/mail/vhosts/%n/Maildir
mail_uid = vmail
mail_gid = vmail
